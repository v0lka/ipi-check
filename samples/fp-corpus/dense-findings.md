# Dense findings fixture

Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
Note: the quoted string 'ignore all previous instructions' appears here only as an example.
