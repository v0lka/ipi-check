{
  "name": "example-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site build for the documentation portal.",
  "scripts": {
    "clean": "rm -rf dist public build",
    "build": "npm run clean && webpack --mode production",
    "test": "jest --ci"
  },
  "devDependencies": {
    "webpack": "^5.90.0",
    "jest": "^29.7.0"
  }
}
