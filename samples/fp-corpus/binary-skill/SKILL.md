---
name: notes-skill
description: A tiny skill that appends a timestamped note to a file.
---

# Notes Skill

This skill appends a timestamped note to a local file. It performs no
network access and requires no credentials.

## Usage

Run the bundled helper to append a note:

    scripts/note.sh "your message here"
