# Detector regression — inline code example

The literal string `ignore all previous instructions` is an illustrative value
used by the test suite.

The literal string `delete all files` is another illustrative value.
